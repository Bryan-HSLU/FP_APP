# projects/ – lokale Projektdaten (NICHT versioniert)
`<uuid>/` mit projekt.json (inkl. retentionUntil), raum.json, stilprofil.json,
plan-v<N>.json, exporte/ – dateibasierte Persistenz (Engineering-Grundlagen §2).
Inhalte bleiben lokal (Privacy, ADR-0009); .gitignore schliesst sie aus.
