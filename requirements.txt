# Streamlit Community Cloud requirements (CPU-only, ~1 GB RAM limit)
# Heavy optional deps (torch, open3d, mast3r) are NOT included — use local install.
pydantic>=2.6
numpy>=1.26
ortools>=9.8
matplotlib>=3.8
trimesh>=4.0
scipy>=1.11
pygltflib>=1.16
groq>=0.8
python-dotenv>=1.0
streamlit>=1.34
